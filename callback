function fetchData(callback) {
  setTimeout(() => {
    const data = { id: 1, name: "Test" };
    callback(data); // Invoking the callback with the data
  }, 1000);
}
fetchData((result) => {
  console.log("Data received:", result);
});
